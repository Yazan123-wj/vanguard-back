[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "gunicorn vanguard_backend.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
releaseCommand = "python manage.py migrate --noinput"
restartPolicyType = "ON_FAILURE"
