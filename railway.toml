[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'python manage.py migrate --noinput && gunicorn vanguard_backend.wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers 2 --timeout 60'"
restartPolicyType = "ON_FAILURE"
